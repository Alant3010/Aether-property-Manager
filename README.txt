Aether Property Manager

1. Run SUPABASE_SQL_RUN_FIRST.sql in Supabase SQL Editor.
2. Deploy this folder to any React host, or run locally:
   npm install
   npm run dev

Important: the Supabase URL is backend only. Open the hosted app URL, not the Supabase URL.
